This project demonstrates the use of **annotation-based autowiring** in Spring Framework.
It covers how to use annotations like `@Autowired`, `@Component`, `@Service`, and `@Repository`
to automatically inject dependencies without XML configuration.

**Key Concepts:**
- @Autowired annotation
- @Component, @Service, @Repository stereotypes
- Annotation-based dependency injection
