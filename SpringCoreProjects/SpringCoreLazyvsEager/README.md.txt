This project demonstrates the difference between **Lazy** and **Eager** initialization
of Spring beans. It shows when beans are instantiated and how to control bean
initialization using `@Lazy` annotation.

**Key Concepts:**
- Eager initialization (default behavior)
- Lazy initialization using @Lazy
- Bean lifecycle and instantiation
