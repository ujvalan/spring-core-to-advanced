This project demonstrates **pure Java-based configuration** in Spring Framework
without any XML. It uses `@Configuration` and `@Bean` annotations to define
and wire beans entirely through Java code.

**Key Concepts:**
- @Configuration annotation
- @Bean annotation
- Java-based Spring container setup
- AnnotationConfigApplicationContext
